# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

includes:
  validate:
    taskfile: "{{.XDG_DATA_HOME}}/automation/home-ops/scripts/.taskfiles/validate.yaml"
    dir: "{{.XDG_DATA_HOME}}/automation/home-ops"
