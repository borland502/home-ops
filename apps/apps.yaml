# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

includes:

    watchyourlan-api:
        taskfile: "./watchyourlan-api/watchyourlan-api.yaml"
        dir: "./watchyourlan-api"

tasks:
    build:
        desc: "Build all nodejs applications"
        cmds:
            - task: watchyourlan-api:build

    install:
        desc: "Install all nodejs applications"
        cmds:
            - task: watchyourlan-api:install
