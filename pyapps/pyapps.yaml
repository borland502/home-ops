# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

includes:
  tk:
    taskfile: "./trapper-keeper/trapper-keeper.yaml"
    dir: "./trapper-keeper"

  ac:
    taskfile: "./ansible-commands/ansible-commands.yaml"
    dir: "./ansible-commands"
